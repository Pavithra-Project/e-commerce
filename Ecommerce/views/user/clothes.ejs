<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Men's Clothing Collection</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background-color: #fdfdfd;
      color: #333;
    }

    header {
      background-color: #fff;
      padding: 20px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
      position: sticky;
      top: 0;
      z-index: 100;
    }

    header h1 {
      font-size: 28px;
      color: #222;
    }

    .nav-buttons button {
      margin-left: 12px;
      padding: 8px 16px;
      border: 1px solid #333;
      background-color: transparent;
      color: #333;
      border-radius: 6px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .nav-buttons button:hover {
      background-color: #333;
      color: white;
    }

    .main-content {
      display: flex;
      flex-wrap: wrap;
      padding: 40px;
      gap: 40px;
    }

    .clothes {
      flex: 1 1 60%;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 24px;
    }

    .clothes-item {
      background-color: #fff;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.05);
      transition: transform 0.2s ease;
      text-align: center;
    }

    .clothes-item:hover {
      transform: translateY(-4px);
    }

    .clothes-item img {
      width: 100%;
      max-height: 220px;
      object-fit: contain;
      border-radius: 8px;
    }

    .clothes-item h3 {
      margin: 12px 0 6px;
      font-size: 20px;
      color: #111;
    }

    .clothes-item p {
      margin: 4px 0;
    }

    .price {
      font-weight: bold;
      font-size: 18px;
      color: #2c3e50;
    }

    .clothes-item form,
    .clothes-item button {
      margin-top: 10px;
    }

    .clothes-item input[type="number"] {
      width: 60px;
      padding: 6px;
      border-radius: 4px;
      border: 1px solid #ccc;
      margin-top: 6px;
    }

    button {
      background-color: #2c3e50;
      color: #fff;
      border: none;
      padding: 10px 18px;
      border-radius: 6px;
      cursor: pointer;
      transition: background 0.3s ease;
    }

    button:hover {
      background-color: #1a252f;
    }

   .hero-image {
  width: 100%;
  height: 300px;
  overflow: hidden;
}

.hero-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

#buyNowModal {
      display: none;
      position: fixed;
      z-index: 999;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background-color: rgba(0,0,0,0.6);
      justify-content: center;
      align-items: center;
    }
    #buyNowModal .modal-content {
      background: white;
      padding: 20px 25px;
      border-radius: 10px;
      max-width: 400px;
      width: 90%;
      position: relative;
      font-size: 14px;
    }
    #buyNowModal .close {
      position: absolute;
      top: 12px;
      right: 16px;
      font-size: 24px;
      cursor: pointer;
      color: #555;
      font-weight: bold;
    }
    #buyNowModal img {
      max-width: 100%;
      height: 120px;
      object-fit: contain;
      border-radius: 8px;
      margin-bottom: 12px;
    }
    #buyNowModal input, #buyNowModal textarea {
      width: 100%;
      padding: 8px;
      margin-top: 8px;
      font-size: 14px;
      border-radius: 5px;
      border: 1px solid #ccc;
      resize: vertical;
    }
    #buyNowModal button {
      width: 100%;
      margin-top: 15px;
      padding: 12px;
      font-size: 16px;
      font-weight: bold;
    }
    .modal-title {
      font-size: 20px;
      margin-bottom: 15px;
      color: #007bff;
      text-align: center;
    }
    .hero-description {
  text-align: center;
  padding: 20px;
  background-color: #f9f9f9;
}

.hero-description h2 {
  font-size: 2rem;
  margin-bottom: 10px;
  color: #333;
}

.hero-description p {
  font-size: 1rem;
  color: #555;
  max-width: 800px;
  margin: 0 auto;
}


    @media (max-width: 900px) {
      .main-content {
        flex-direction: column;
      }
    }
  </style>
</head>
<body>


  <header>
    <h1>Men's Clothing</h1>
    <div class="nav-buttons">
       <button onclick="location.href='/user/logout'">Logout</button>
      <button onclick="location.href='/user/cart'">My Cart</button>
      <button onclick="location.href='/user/order'">My Orders</button>
    </div>
  </header>
   <div class="hero-image">
      <img src="https://media.istockphoto.com/id/1144368446/photo/men-formal-wear-classy-outfit-business-suit.jpg?s=612x612&w=0&k=20&c=9ohd6ub6gdLTNdYtW2Cc64zTVHj0eX_eknN_lQGs4DE=" alt="Men's Fashion">
    </div>
    <div class="hero-description">
  <h2>Step Into Style</h2>
  <p>Explore our curated selection of premium men's fashion. From casual essentials to timeless classics, we've got everything you need to upgrade your wardrobe.</p>
</div>

  </div>

 
  <div class="main-content">
   
    <div class="clothes">
      <% clothes.forEach(function(item) { %>
        <div class="clothes-item">
          <img src="<%= item.image %>" alt="<%= item.name %>">
          <h3><%= item.name %></h3>
          <p>Brand: <%= item.brand %></p>
          <p class="price">₹<%= item.price %></p>

          
          <form action="/add-to-cart/<%= item._id %>" method="POST">
            <input type="number" name="quantity" min="1" value="1" required>
            <button type="submit">Add to Cart</button>
          </form>

          <button onclick="openModal('<%= item._id %>', '<%= item.name %>', '<%= item.image %>', '<%= item.price %>')">Buy Now</button>
        </div>
      <% }) %>
    </div>

   
  <div id="buyNowModal">
    <div class="modal-content">
      <span class="close" onclick="closeModal()">&times;</span>
      <div class="modal-title">Complete Your Order</div>

      <form action="/user/order" method="POST">
        <input type="hidden" name="productId" id="modalProductId">
        <img id="modalProductImage" src="" alt="Product Image" style="max-width: 100%; margin-bottom: 10px;">
        <p><strong>Product:</strong> <span id="modalName"></span></p>
        <p><strong>Price:</strong> ₹<span id="modalPrice"></span></p>

        <input type="text" name="customerName" placeholder="Your Name" required>
        <input type="text" name="contact" placeholder="Contact Number" required>
        <textarea name="address" placeholder="Delivery Address" required></textarea>
        
        <input type="number" name="quantity" min="1" value="1" required oninput="updateTotalPrice()">

        <p>Total: ₹<span id="modalTotalPrice">0</span></p>

        <button type="submit">Place Order</button>
      </form>
    </div>
  </div>

 
  <script>
    function openModal(id, name, image, price) {
      document.getElementById("buyNowModal").style.display = "flex";
      document.getElementById("modalProductId").value = id;
      document.getElementById("modalName").innerText = name;
      document.getElementById("modalPrice").innerText = price;
      document.getElementById("modalProductImage").src = image;
      updateTotalPrice();
    }

    function closeModal() {
      document.getElementById("buyNowModal").style.display = "none";
    }

    function updateTotalPrice() {
      const price = parseFloat(document.getElementById("modalPrice").innerText);
      const quantity = document.querySelector('#buyNowModal input[name="quantity"]').value;
      const totalPrice = price * quantity;
      document.getElementById("modalTotalPrice").innerText = totalPrice.toFixed(2);
    }
  </script>

</body>
</html>
