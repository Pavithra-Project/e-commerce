<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Watch Collection</title>
  <style>
    * {
      margin: 0; padding: 0; box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background-color: #fdfdfd;
      color: #333;
      min-height: 100vh;
    }

    header {
      background-color: #fff;
      padding: 20px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
      position: sticky;
      top: 0;
      z-index: 100;
    }

    header h1 {
      color: #1a1a1a;
      font-size: 28px;
      font-weight: bold;
    }

    .nav-buttons button {
      background-color: #000;
      color: white;
      border: none;
      padding: 10px 16px;
      border-radius: 10px;
      margin-left: 10px;
      cursor: pointer;
      font-weight: 600;
      transition: 0.3s;
    }

    .nav-buttons button:hover {
      background-color: #444;
    }

    .hero-image {
      width: 100%;
      height: 280px;
      background-image: url('https://doxawatches.com/cdn/shop/files/image_2_website_desktop.png?v=1739277300&width=1920');
      background-position: center;
      background-size: cover;
      filter: brightness(0.9);
    }

    .offer-banner {
      background: #000;
      color: #fcd144;
      text-align: center;
      padding: 12px;
      font-size: 18px;
      font-weight: bold;
      letter-spacing: 1px;
    }

    .intro-text {
      text-align: center;
      margin: 40px 0;
      padding: 0 20px;
    }

    .intro-text h2 {
      font-size: 32px;
      color: #222;
      margin-bottom: 10px;
    }

    .intro-text p {
      font-size: 18px;
      color: #666;
      max-width: 700px;
      margin: auto;
    }

    .products {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 30px;
      padding: 0 40px 60px 40px;
      max-width: 1200px;
      margin: auto;
    }

    .product-card {
      background: white;
      border-radius: 16px;
      padding: 20px;
      text-align: center;
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s;
    }

    .product-card:hover {
      transform: translateY(-8px);
    }

    .product-card img {
      width: 100%;
      height: 230px;
      object-fit: cover;
      border-radius: 14px;
      margin-bottom: 15px;
    }

    .product-card h3 {
      font-size: 20px;
      color: #111;
      margin-bottom: 6px;
    }

    .product-card p {
      font-size: 15px;
      color: #555;
      margin-bottom: 4px;
    }

    .price {
      font-weight: bold;
      font-size: 18px;
      color: #0a0a0a;
      margin: 10px 0;
    }

    form, button {
      margin-top: 10px;
    }

    input[type="number"] {
      width: 60px;
      padding: 6px;
      font-size: 14px;
      border-radius: 6px;
      border: 1px solid #ccc;
      margin-right: 8px;
    }

    button {
      background-color: #000;
      color: white;
      padding: 10px 18px;
      border: none;
      border-radius: 8px;
      font-size: 15px;
      cursor: pointer;
      font-weight: 600;
    }

    button:hover {
      background-color: #222;
    }

    
    #buyNowModal {
      display: none;
      position: fixed;
      z-index: 999;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background-color: rgba(0,0,0,0.6);
      justify-content: center;
      align-items: center;
    }
    #buyNowModal .modal-content {
      background: white;
      padding: 20px 25px;
      border-radius: 10px;
      max-width: 400px;
      width: 90%;
      position: relative;
      font-size: 14px;
    }
    #buyNowModal .close {
      position: absolute;
      top: 12px;
      right: 16px;
      font-size: 24px;
      cursor: pointer;
      color: #555;
      font-weight: bold;
    }
    #buyNowModal img {
      max-width: 100%;
      height: 120px;
      object-fit: contain;
      border-radius: 8px;
      margin-bottom: 12px;
    }
    #buyNowModal input, #buyNowModal textarea {
      width: 100%;
      padding: 8px;
      margin-top: 8px;
      font-size: 14px;
      border-radius: 5px;
      border: 1px solid #ccc;
      resize: vertical;
    }
    #buyNowModal button {
      width: 100%;
      margin-top: 15px;
      padding: 12px;
      font-size: 16px;
      font-weight: bold;
    }
    .modal-title {
      font-size: 20px;
      margin-bottom: 15px;
      color: #007bff;
      text-align: center;
    }
  </style>
</head>
<body>

  <header>
    <h1>Watch Collection</h1>
    <div class="nav-buttons">
         <button onclick="location.href='/user/logout'">Logout</button>
      <button onclick="location.href='/user/cart'">My Cart</button>
      <button onclick="location.href='/user/order'">My Orders</button>
    </div>
  </header>

  <div class="hero-image"></div>
  <div class="offer-banner">🔥 Summer Sale: Up to 50% Off on Premium Watches!</div>

  <section class="intro-text">
    <h2>Timeless Elegance on Your Wrist</h2>
    <p>Explore our curated collection of premium watches that blend tradition, innovation, and unbeatable style. Perfect for every moment.</p>
  </section>

  <div class="products">
    <% watches.forEach(function(item) { %>
      <div class="product-card">
        <img src="<%= item.image %>" alt="<%= item.name %>">
        <h3><%= item.name %></h3>
        <p>Brand: <%= item.brand %></p>
        <p class="price">₹<%= item.price %></p>

        <form action="/add-to-cart/<%= item._id %>" method="POST">
          <input type="number" name="quantity" min="1" value="1" required>
          <button type="submit">Add to Cart</button>
        </form>
        <button onclick="openModal('<%= item._id %>', '<%= item.name %>', '<%= item.image %>', '<%= item.price %>')">Buy Now</button>
      </div>
    <% }) %>
  </div>

  <div id="buyNowModal" class="modal">
    <div class="modal-content">
      <span class="close" onclick="closeModal()">&times;</span>
      <h3>Place Your Order</h3>
      <form action="/user/order" method="POST">
        <input type="hidden" name="productId" id="modalProductId">
        <img id="modalProductImage" src="" style="width: 100%; border-radius: 12px; margin: 12px 0;" />
        <p><strong id="modalName"></strong></p>
        <p class="price">Price: ₹<span id="modalPrice"></span></p>

        <input type="text" name="customerName" placeholder="Your Name" required />
        <input type="tel" name="contactNumber" placeholder="Contact Number" pattern="[0-9]{10}" required />
        <textarea name="address" placeholder="Shipping Address" required></textarea>
        <input type="number" name="quantity" id="modalQuantity" min="1" value="1" required />
        <button type="submit">Place Order</button>
      </form>
    </div>
  </div>

  <script>
    function openModal(id, name, img, price) {
      document.getElementById('buyNowModal').style.display = 'flex';
      document.getElementById('modalProductId').value = id;
      document.getElementById('modalName').innerText = name;
      document.getElementById('modalProductImage').src = img;
      document.getElementById('modalPrice').innerText = price;
      document.getElementById('modalQuantity').value = 1;
    }

    function closeModal() {
      document.getElementById('buyNowModal').style.display = 'none';
    }

    window.onclick = function(event) {
      const modal = document.getElementById('buyNowModal');
      if (event.target === modal) {
        closeModal();
      }
    }
  </script>

</body>
</html>
